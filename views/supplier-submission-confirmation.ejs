<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Confirmation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
        }

        .logo {
            font-size: 2em;
            font-weight: bold;
        }

        .container {
            margin-top: 50px;
        }

        h1 {
            font-size: 2.5em;
            color: #333; /* Changed to black */
        }

        p {
            margin-bottom: 40px;
            font-size: 1.2em;
        }

        .button-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 20px;
        }

        a {
            display: inline-block;
            padding: 15px 20px;
            font-size: 1.2em;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 8px;
            margin: 10px 0;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
            text-decoration: none;
            width: 200px;
            text-align: center;
        }

        a:hover {
            background-color: #45a049;
            transform: translateY(-2px);
            box-shadow: 0px 6px 8px rgba(0, 0, 0, 0.2);
        }

        footer {
            margin-top: 50px;
            color: #888;
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">SupplierDB</div>
    </header>

    <div class="container">
        <h1>Submission Confirmed</h1>
        <p>Your submission for <strong><%= companyName %></strong> has been successfully recorded.</p>

        <div class="button-container">
            <a href="/">Return to Home</a>
        </div>
    </div>

    <footer>
        <p>&copy; 2024 SupplierDB. All rights reserved.</p>
    </footer>

</body>
</html>






