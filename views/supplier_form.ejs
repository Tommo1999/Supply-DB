<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Supplier Form for <%= companyName %></title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            text-align: center;
        }
        header {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
        }
        .logo {
            font-size: 2em;
            font-weight: bold;
        }
        .container {
            margin-top: 50px;
            max-width: 600px;
            margin: 20px auto;
            background: #fff;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            padding-left: 40px;
            padding-right: 40px;
        }
        h1 {
            color: #333;
            font-size: 2.5em;
        }
        label {
            display: block;
            margin: 10px 0 5px;
            text-align: left;
        }
        input[type="text"],
        input[type="email"],
        input[type="tel"],
        input[type="url"],
        textarea {
            width: 100%;
            padding: 10px;
            margin: 5px 0 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
            display: block;
            font-size: 1em;
            box-sizing: border-box; /* Ensures padding is included in the width */
        }
        textarea {
            height: 50px; /* Same height as other textboxes */
            resize: none; /* Prevent resizing */
        }
        button {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1.2em;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
        .hint {
            font-size: 12px;
            color: #888;
            margin-top: 5px;
            text-align: left;
        }
        footer {
            margin-top: 50px;
            color: #888;
        }
        .form-group {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

<header>
    <div class="logo">SupplierDB</div>
</header>

<div class="container">
    <h1>Supplier Form for <%= companyName %></h1>
    
    <!-- Supplier form -->
    <form action="/submit/<%= companyName %>" method="POST">
        
        <div class="form-group">
            <label for="supplierID">Supplier ID</label>
            <input type="text" id="supplierID" name="supplierID" required>
        </div>

        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required>
        </div>

        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required placeholder="e.g. john.doe@example.com">
            <p class="hint">Include the "@" symbol. Example: john.doe@example.com</p>
        </div>

        <div class="form-group">
            <label for="company_phone_number">Company Phone Number</label>
            <input type="tel" id="company_phone_number" name="company_phone_number" required>
        </div>

        <div class="form-group">
            <label for="mobile_phone_number">Mobile Phone Number (Optional)</label>
            <input type="tel" id="mobile_phone_number" name="mobile_phone_number">
        </div>

        <div class="form-group">
            <label for="core_business">Core Business</label>
            <textarea id="core_business" name="core_business" required></textarea>
        </div>

        <div class="form-group">
            <label for="website">Website (Optional)</label>
            <input type="text" id="website" name="website" placeholder="e.g. www.example.com">
            <p class="hint">Include "www.". Example: www.example.com</p>
        </div>

        <div class="form-group">
            <label for="postcode">Postal Code (Optional)</label>
            <input type="text" id="postcode" name="postcode">
        </div>

        <button type="submit">Submit Supplier</button>
    </form>
</div>

<footer>
    <p>&copy; 2024 SupplierDB. All rights reserved.</p>
</footer>

</body>
</html>




