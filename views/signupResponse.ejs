<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign-Up Success</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        header {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            display: flex;
            align-items: center;
            position: relative;
            height: 60px; /* Consistent header height */
        }

        .logo {
            font-size: 1.8em;
            font-weight: bold;
            margin: 0 auto; /* Center the logo */
        }

        /* Home button styling */
        .home-button {
            position: absolute;
            left: 10px; /* Position the button on the left */
            top: 50%;
            transform: translateY(-50%); /* Center vertically */
            width: 35px; /* Adjust size */
            height: 35px;
            background-color: white;
            color: #4CAF50;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
            font-size: 1em;
            text-decoration: none;
        }

        .home-button:hover {
            background-color: #f1f1f1;
            transform: translateY(-50%) scale(1.1);
        }

        .container {
            margin-top: 50px;
        }

        h1 {
            color: #333;
            font-size: 2.5em;
        }

        p {
            font-size: 1.2em;
            color: #555;
        }

        footer {
            margin-top: 50px;
            color: #888;
        }

        /* Style for the copyable link */
        .copyable-link {
            font-family: monospace;
            background-color: #f4f4f4;
            padding: 10px;
            border-radius: 4px;
            word-wrap: break-word;
            margin-top: 20px;
            display: inline-block;
        }
    </style>
</head>
<body>
    <header>
        <!-- Home Page Button -->
        <a href="/" class="home-button" title="Go to Home Page">
            <i class="fas fa-home"></i>
        </a>
        <div class="logo">SupplierDB</div>
    </header>

    <div class="container">
        <h1>Welcome!</h1>
        <p>Thank you for signing up. Your company has been successfully registered.</p>
        <p>You will receive an email with your custom URL link to share with your suppliers.</p> 
        <p>Or share this link with your suppliers:</p>
        <pre class="copyable-link"><%= herokuUrl + customPath %></pre> <!-- Display the custom URL here -->
    </div>

    <footer>
        <p>&copy; 2024 SupplierDB. All rights reserved.</p>
    </footer>
</body>
</html>
